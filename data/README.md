# Data organization

This directory separates source data (`raw/`) from derived and transformed data (`processed/`). Metadata templates document provenance, licensing, dates, CRS, processing, and limitations. Do not commit credentials or unapproved restricted data. Large datasets should normally remain outside Git and be referenced by stable metadata and checksums.
